{
  "name": "ppob-toss-dream",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node --test",
    "build:web": "vite build",
    "build": "vite build && ait build",
    "preview": "vite preview",
    "ait:build": "ait build",
    "ait:deploy": "ait deploy",
    "deploy": "ait deploy"
  },
  "dependencies": {
    "@apps-in-toss/web-framework": "^3.0.3"
  },
  "devDependencies": {
    "@apps-in-toss/devtools": "^3.0.3",
    "vite": "^5.4.11"
  }
}
